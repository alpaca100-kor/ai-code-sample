# 웹 브라우저 기반 JavaScript 앱 제작 요청

## 1. 개발 환경

- 언어 : HTML / CSS / JavaScript
- 프레임워크 : Vanilla JS
- 파일 구성 : 단일 HTML 파일
  - `index.html` 하나만 사용
  - CSS는 `<style>` 태그 내부에 작성
  - JavaScript는 `<script>` 태그 내부에 작성
  - 별도의 빌드 과정 없이 브라우저에서 바로 실행 가능하도록 작성
- 외부 라이브러리 :
  - 기본 이미지 편집 기능은 외부 라이브러리를 사용하지 않고 브라우저 기본 API로 구현
  - Canvas API를 이용하여 이미지 병합, 회전, 반전, 크기 조정, 자르기 및 미리보기 구현
  - File API / Blob API를 이용하여 이미지 파일 읽기 및 저장
  - ICO 생성 기능은 JavaScript로 ICO 파일 구조를 생성하거나, 반드시 필요한 경우에만 경량 라이브러리를 사용
  - TIFF 입력 지원이 브라우저 기본 기능으로 충분하지 않은 경우 별도의 TIFF 디코더를 사용
  - 외부 CDN에 의존하지 않는 것을 우선하여 완전한 오프라인 실행이 가능하도록 설계
- 지원 브라우저 :
  - 최신 Chrome
  - 최신 Microsoft Edge
  - 최신 Safari
  - 최신 Firefox
  - 구버전 브라우저는 별도 지원하지 않음
- 인터넷 연결 필요 여부 :
  - 인터넷 연결 없이도 핵심 기능을 사용할 수 있어야 함
  - 이미지 파일은 서버로 업로드하지 않고 브라우저 내부에서 처리
  - GitHub Pages 등 정적 웹 호스팅에서도 동일하게 동작해야 함

------------------------------------------------

## 2. 프로그램 목적

알파카 이미지 편집기는 별도의 이미지 편집 프로그램을 설치하지 않고 웹 브라우저에서 간단한 이미지 편집 및 변환 작업을 수행할 수 있는 클라이언트 사이드 이미지 편집 웹 앱이다.

기존 Python/Tkinter 기반 알파카 이미지 편집기의 주요 기능을 웹 브라우저 환경으로 이전한다.

사용자가 선택한 이미지 파일을 브라우저 내부에서 처리하며, 이미지 데이터를 서버로 전송하지 않는 것을 기본 원칙으로 한다.

주요 목적은 여러 이미지의 병합, 이미지 뒤집기, 회전, 크기 조정, 자르기 및 ICO 파일 변환과 같은 반복적인 이미지 작업을 간단한 UI를 통해 빠르게 수행할 수 있도록 하는 것이다.

별도의 회원가입, 로그인, 서버 저장 또는 데이터베이스는 사용하지 않는다.

------------------------------------------------

## 3. 주요 기능

### 필수 기능

① 이미지 병합

다음 5가지 병합 모드를 제공한다.

- 2개 이미지 가로 병합
- 2개 이미지 세로 병합
- 3개 이미지 가로 병합
- 3개 이미지 세로 병합
- 4개 이미지 2×2 격자 병합

이미지 병합 시 다음 옵션을 제공한다.

- 이미지 크기 기준
  - 작은 사이즈에 맞추기
  - 큰 사이즈에 맞추기
  - 이미지 크기 변경하지 않고 병합
- 여백 크기(px)
- 여백 색상
- 테두리 굵기(px)
- 테두리 색상
- 이미지 비율 차이로 발생하는 빈 공간의 채우기 색상

이미지 자체의 투명 영역은 가능한 한 유지한다.

② 이미지 뒤집기

하나의 이미지를 선택하여 다음 작업을 수행한다.

- 좌우 뒤집기
- 상하 뒤집기
- 상하 및 좌우 뒤집기

③ 이미지 회전

하나의 이미지를 선택하여 다음 각도로 회전한다.

- 시계 방향 90°
- 시계 방향 180°
- 시계 방향 270°

④ 이미지 크기 조정

다음 두 가지 방법을 제공한다.

- 비율로 크기 조정
- 너비/높이를 픽셀 단위로 직접 입력

비율 조정 기능에는 다음을 제공한다.

- 1~100% 범위 슬라이더
- 비율 직접 입력
- 25% / 50% / 75% 빠른 선택
- 변경 후 예상 결과 크기 표시

직접 입력 방식에는 다음을 제공한다.

- 너비(W)
- 높이(H)
- 비율 유지 옵션

원본 이미지의 크기, 파일 크기, 파일 형식 등의 정보를 표시한다.

저장 품질 설정을 제공한다.

- 10~100% 품질 조절
- JPEG 및 WebP 저장 시 적용
- PNG 저장 시 품질 옵션은 적용하지 않음

⑤ 이미지 자르기

하나의 이미지를 선택하여 자르기 영역을 지정한다.

- 이미지 전체를 초기 자르기 영역으로 설정
- 8방향 크롭 핸들 제공
  - 좌상단
  - 상단
  - 우상단
  - 좌측
  - 우측
  - 좌하단
  - 하단
  - 우하단
- 자르기 영역 자체를 마우스로 드래그하여 이동
- 자르기 영역을 이미지 범위 밖으로 이동할 수 없도록 제한
- 최소 자르기 영역 크기 설정
- 현재 선택 영역의 실제 원본 픽셀 크기 표시
- 영역 초기화 기능 제공

⑥ ICO 파일 변환

이미지를 ICO 파일로 변환한다.

입력 지원 형식:

- PNG
- JPG/JPEG
- BMP
- GIF
- TIFF
- WebP

ICO에는 다음 해상도를 포함한다.

- 16×16
- 32×32
- 48×48
- 64×64
- 128×128
- 256×256

원본 이미지의 비율을 유지하면서 정사각형 캔버스 중앙에 배치한다.

정사각형 캔버스에서 남는 영역은 투명하게 처리한다.

원본 이미지의 투명 배경 여부를 표시한다.

원본 이미지가 256×256보다 작은 경우 확대될 수 있다는 경고를 표시한다.

⑦ 이미지 미리보기

현재 설정으로 생성될 결과 이미지를 미리보기로 확인할 수 있어야 한다.

미리보기에서는 이미지 비율을 유지한다.

큰 이미지는 화면 영역에 맞게 축소해서 표시한다.

이미지의 실제 크기와 미리보기 배율을 표시한다.

투명 이미지의 경우 체크무늬 배경을 사용하여 투명 영역을 쉽게 확인할 수 있도록 한다.

⑧ 결과 이미지 저장

결과 이미지를 브라우저에서 다운로드할 수 있도록 한다.

지원 형식:

- PNG
- JPEG/JPG
- WebP
- BMP
- ICO

기능별로 적절한 기본 파일명을 자동 생성한다.

예:

- `image_merged.png`
- `image_flipped.png`
- `image_rotated.png`
- `image_resized.png`
- `image_cropped.png`
- `image.ico`

### 선택 기능

① 파일 드래그 앤 드롭

이미지 선택 영역에 파일을 끌어다 놓아 이미지를 불러올 수 있도록 한다.

여러 이미지가 필요한 병합 기능에서는 각 이미지 슬롯에 개별적으로 드래그 앤 드롭할 수 있도록 한다.

② 결과 이미지 정보 표시

결과 이미지 생성 후 다음 정보를 표시한다.

- 결과 이미지 크기
- 결과 파일 형식
- 예상 파일 크기
- 현재 작업 모드

③ 클립보드 이미지 붙여넣기

브라우저가 지원하는 경우 `Ctrl + V`를 이용하여 클립보드에 있는 이미지를 불러올 수 있도록 한다.

④ 최근 작업 설정 유지

새로고침 전까지 현재 선택한 옵션을 유지한다.

⑤ 다크 모드

브라우저의 `prefers-color-scheme` 설정을 활용하여 다크 모드를 제공할 수 있다.

------------------------------------------------

## 4. 사용자 작업 순서

1. 웹 앱을 실행한다.
2. 좌측 기능 메뉴에서 원하는 이미지 편집 기능을 선택한다.
3. 이미지 선택 영역에서 파일을 선택하거나 파일을 드래그 앤 드롭한다.
4. 선택한 기능에 맞는 옵션을 설정한다.
5. `미리보기` 버튼을 클릭하여 결과를 확인한다.
6. 설정이 적절하면 `다운로드` 또는 `실행` 버튼을 클릭한다.
7. 브라우저에서 생성된 결과 이미지 파일을 다운로드한다.

병합 기능의 경우 필요한 이미지 수만큼 이미지를 모두 선택한 후 병합 옵션을 설정한다.

자르기 기능의 경우 이미지가 표시된 캔버스에서 크롭 영역의 핸들을 직접 조작한다.

------------------------------------------------

## 5. 화면 구성 (레이아웃)

### 상단

헤더 영역을 배치한다.

- 앱 이름 : 알파카 이미지 편집기
- 간단한 앱 설명
- 필요한 경우 도움말 버튼

### 중앙

화면을 좌우 2개 영역으로 구성한다.

#### 좌측 : 기능 선택 사이드바

다음 기능을 목록 형태로 표시한다.

- 2개 이미지 병합 (가로)
- 2개 이미지 병합 (세로)
- 3개 이미지 병합 (가로)
- 3개 이미지 병합 (세로)
- 4개 이미지 병합 (2×2)
- 이미지 뒤집기
- 이미지 회전하기
- 이미지 크기 조정
- 이미지 자르기
- ICO 파일 변환

현재 선택된 기능은 강조 표시한다.

#### 우측 : 작업 영역

현재 선택된 기능에 필요한 옵션 UI를 동적으로 표시한다.

이미지 선택 영역

원본 이미지 정보

작업 옵션

미리보기 영역

상태 메시지

등을 카드 형태로 배치한다.

### 하단

공통 작업 버튼을 배치한다.

- 미리보기
- 실행/다운로드

웹 앱에서는 데스크톱 앱의 `종료` 버튼을 사용하지 않는다.

하단에는 다음과 같은 상태 정보도 표시할 수 있다.

- 이미지 로드 완료
- 작업 완료
- 오류 발생
- 다운로드 준비 완료

### 반응형 대응

- 모바일 대응 필요 여부 : 필요
- 데스크톱에서는 좌측 사이드바 + 우측 작업 영역 구조를 유지한다.
- 태블릿에서는 사이드바 폭을 축소한다.
- 모바일에서는 기능 선택 메뉴를 상단 드롭다운 또는 접이식 메뉴로 변경한다.
- 이미지 미리보기 영역은 화면 너비에 맞게 자동 조정한다.
- 크롭 핸들은 터치 환경에서도 조작할 수 있도록 충분한 크기를 확보한다.
- 버튼과 입력 컨트롤은 터치하기 쉬운 최소 크기를 유지한다.

------------------------------------------------

## 6. 세부 동작

### 기능 메뉴 선택

기능 메뉴를 클릭하면

↓

현재 선택된 기능을 활성화한다.

↓

우측 옵션 패널을 해당 기능에 맞게 변경한다.

↓

기존 기능의 입력 상태와 무관하게 새 기능에 필요한 UI를 표시한다.

### 이미지 파일 선택

`이미지 선택` 버튼을 클릭하면

↓

브라우저의 파일 선택 창을 연다.

↓

이미지 파일을 선택한다.

↓

File 객체를 메모리에 저장한다.

↓

`FileReader`, `createImageBitmap()` 또는 `HTMLImageElement`를 이용하여 이미지를 읽는다.

↓

이미지의 크기와 형식 정보를 표시한다.

### 이미지 드래그 앤 드롭

파일을 이미지 영역에 드래그하면

↓

드롭 영역을 강조한다.

↓

파일을 놓으면 이미지 파일인지 확인한다.

↓

지원하지 않는 파일이면 오류 메시지를 표시한다.

↓

지원되는 이미지라면 해당 이미지 슬롯에 등록한다.

### 이미지 병합

이미지를 필요한 개수만큼 선택하면

↓

각 이미지를 Canvas에서 처리한다.

↓

크기 기준에 따라 이미지 크기를 조정한다.

↓

설정된 여백과 색상을 적용한다.

↓

이미지를 가로/세로/2×2 형태로 배치한다.

↓

필요한 경우 테두리를 추가한다.

↓

Canvas에 결과 이미지를 생성한다.

### 이미지 뒤집기

이미지를 선택한다.

↓

좌우/상하/양방향 중 하나를 선택한다.

↓

Canvas transform을 이용하여 이미지를 반전한다.

↓

결과를 미리보기한다.

### 이미지 회전

이미지를 선택한다.

↓

회전 각도를 선택한다.

↓

Canvas를 회전 변환한다.

↓

90° 또는 270° 회전 시 캔버스의 가로/세로 크기를 교환한다.

↓

결과를 미리보기한다.

### 이미지 크기 조정

이미지를 선택한다.

↓

비율 또는 직접 입력 방식을 선택한다.

↓

새로운 크기를 계산한다.

↓

Canvas에 새로운 크기로 이미지를 렌더링한다.

↓

LANCZOS에 준하는 고품질 브라우저 리사이즈 결과를 얻을 수 있도록 Canvas 기반 리사이즈를 사용한다.

### 이미지 자르기

이미지를 선택한다.

↓

Canvas에 이미지 전체를 표시한다.

↓

초기 크롭 영역을 이미지 전체로 설정한다.

↓

8방향 핸들을 표시한다.

↓

사용자가 핸들을 드래그한다.

↓

크롭 영역을 이미지 경계 안으로 제한한다.

↓

선택 영역을 이동할 수 있도록 한다.

↓

Canvas 좌표를 원본 이미지 좌표로 환산한다.

↓

실제 원본 픽셀 기준으로 이미지를 잘라낸다.

### ICO 변환

이미지를 선택한다.

↓

이미지를 RGBA 형태로 처리한다.

↓

256×256 정사각형 영역에 비율을 유지하면서 중앙 배치한다.

↓

남는 영역을 투명하게 처리한다.

↓

16×16, 32×32, 48×48, 64×64, 128×128, 256×256 크기의 이미지를 각각 생성한다.

↓

각 이미지를 PNG 데이터로 변환한다.

↓

ICO 파일 헤더 및 디렉터리 정보를 생성한다.

↓

각 PNG 데이터를 하나의 ICO Blob으로 패키징한다.

↓

`.ico` 파일로 다운로드한다.

### 미리보기

`미리보기` 버튼을 클릭하면

↓

현재 설정을 기준으로 결과 이미지를 생성한다.

↓

미리보기 영역에 결과를 표시한다.

↓

원본 및 결과 이미지 크기를 표시한다.

↓

투명 영역이 있는 경우 체크무늬 배경을 표시한다.

### 다운로드

`다운로드` 버튼을 클릭하면

↓

현재 설정으로 최종 이미지를 생성한다.

↓

Canvas 또는 Blob으로 결과 데이터를 만든다.

↓

선택한 파일 형식으로 변환한다.

↓

Object URL을 생성한다.

↓

가상 다운로드 링크를 실행한다.

↓

사용자의 브라우저에서 결과 파일을 다운로드한다.

------------------------------------------------

## 7. 데이터 저장 및 상태 관리

- 데이터 저장 필요 여부 : 별도 영구 저장 불필요
- 저장 방식 :
  - [ ] 브라우저 저장 (localStorage / IndexedDB)
  - [ ] 서버/백엔드 연동
  - [x] 저장 없음 (세션 중에만 유지)
- 새로고침 시 데이터 유지 필요 여부 : 필요 없음
- 여러 기기/브라우저 간 동기화 필요 여부 : 없음

이미지 파일 자체는 서버에 업로드하지 않는다.

사용자가 선택한 File 객체와 Canvas 결과물은 브라우저 메모리에서만 유지한다.

필요한 경우 다음 설정값만 localStorage에 저장하는 선택 기능을 제공할 수 있다.

- 마지막으로 선택한 기능
- 여백 크기
- 여백 색상
- 테두리 크기
- 테두리 색상
- 기본 크기 조정 비율
- 기본 저장 품질
- 다크 모드 설정

단, 이미지 원본 파일 자체는 localStorage에 저장하지 않는다.

------------------------------------------------

## 8. 디자인 및 스타일

- 디자인 톤 : 미니멀하고 깔끔한 Windows 11 스타일의 데스크톱 웹 앱
- 기본 테마 : 밝은 라이트 테마
- 다크/라이트 모드 지원 여부 : 선택적으로 지원
- 참고할 만한 디자인 :
  - Windows 11 설정 앱
  - Windows 11 기본 이미지 관련 UI
  - 기존 Python 알파카 이미지 편집기의 UI
- 색상 구성 :
  - 전체 배경 : 밝은 회색
  - 카드 : 흰색
  - 테두리 : 밝은 회색
  - 주요 강조색 : 파란색
  - 성공 : 녹색
  - 경고 : 주황색
  - 오류 : 빨간색
- 폰트 :
  - Segoe UI 우선
  - 시스템 기본 sans-serif 폴백
- 아이콘 :
  - 외부 아이콘 라이브러리에 의존하지 않고 Unicode 또는 CSS 기반 아이콘을 우선 사용
  - 필요한 경우 SVG 아이콘을 HTML 내부에 직접 포함
- 카드형 UI를 사용하여 기능별 옵션을 명확하게 구분한다.
- 입력 컨트롤의 크기를 충분히 확보한다.
- 현재 선택된 기능은 명확하게 강조한다.
- 오류 및 상태 메시지는 해당 작업 영역에서 바로 확인할 수 있도록 한다.

------------------------------------------------

## 9. 예외 처리 및 오류 대응

### 입력값이 잘못된 경우

- 음수인 여백 크기 입력 방지
- 음수인 테두리 크기 입력 방지
- 잘못된 이미지 크기 입력 방지
- 이미지 크기는 최소 1px 이상
- 품질은 10~100 범위로 제한
- 비율은 1~100 범위로 제한
- 잘못된 HEX 색상 입력 시 오류 메시지 표시 및 이전 값 유지
- 크롭 영역이 지나치게 작은 경우 작업 중단

### 이미지 파일 오류

- 지원하지 않는 파일 형식
- 손상된 이미지
- 브라우저에서 디코딩할 수 없는 이미지
- 파일 읽기 실패

등을 감지하여 화면 내 오류 메시지를 표시한다.

### 네트워크 오류

핵심 이미지 처리 기능에는 네트워크가 필요하지 않으므로 네트워크 오류가 발생해도 이미지 편집 기능은 계속 사용할 수 있어야 한다.

외부 라이브러리를 CDN으로 불러오는 경우 CDN 연결 실패에 대한 별도 처리가 필요하다.

가능하면 외부 CDN에 의존하지 않는다.

### 데이터가 없을 경우

이미지를 선택하지 않고 실행 버튼을 클릭하면

`처리할 이미지를 먼저 선택해주세요.`

등의 안내 메시지를 표시한다.

병합 기능에서 필요한 이미지가 모두 선택되지 않은 경우 누락된 이미지 번호를 표시한다.

### 오류 메시지 표시 방식

- 기본 : 화면 내 상태 메시지
- 중요 오류 : 모달 또는 alert
- 개발 중 디버깅 : console.error()

사용자에게 불필요한 기술적 오류 메시지를 직접 노출하지 않는다.

------------------------------------------------

## 10. 성능 요구사항

- 대용량 데이터 처리 필요 여부 : 있음
- 대용량 이미지 처리 시 브라우저 메모리 사용량을 고려한다.
- 원본 이미지를 불필요하게 여러 번 복제하지 않는다.
- 미리보기용 이미지와 실제 저장용 이미지를 구분한다.
- 미리보기에서는 화면 크기에 맞게 축소된 이미지를 사용한다.
- 최종 다운로드 시에는 원본 해상도 기준으로 처리한다.
- 목록 가상 스크롤 : 필요 없음
- 이미지/파일 업로드 용량 제한 :
  - 서버 업로드가 아니므로 서버 기준 제한은 없음
  - 다만 브라우저 메모리 문제를 방지하기 위해 매우 큰 이미지는 경고할 수 있음
  - 예: 10,000×10,000px 이상의 이미지 처리 시 경고
- 대형 이미지 처리 중에는 작업 진행 상태를 표시할 수 있도록 한다.
- 반복적인 Canvas 생성 및 ImageBitmap 생성은 최소화한다.

------------------------------------------------

## 11. 접근성 및 사용성

- 키보드 조작 지원 필요 여부 : 필요
- Tab 키를 이용하여 주요 버튼과 입력 컨트롤에 접근할 수 있어야 한다.
- Enter 키로 주요 버튼을 실행할 수 있도록 한다.
- Escape 키로 모달 및 미리보기 창을 닫을 수 있도록 한다.
- 슬라이더는 키보드로 값을 조정할 수 있어야 한다.
- 색상 선택 버튼에는 적절한 aria-label을 제공한다.
- 아이콘만 사용하는 버튼에는 title 및 aria-label을 제공한다.
- 스크린 리더 대응 : 가능한 범위에서 지원
- 상태 메시지는 `aria-live`를 이용하여 변경 사항을 전달한다.
- 충분한 색상 대비를 확보한다.
- 색상만으로 오류/성공 상태를 구분하지 않는다.
- 다국어(i18n) 지원 필요 여부 : 현재 한국어만 지원
- 향후 영어 지원을 고려하여 UI 문자열을 상수 객체로 분리한다.

------------------------------------------------

## 12. 코드 작성 규칙

- 단일 HTML 파일 구조를 사용한다.
- `<style>`과 `<script>`를 HTML 내부에 포함한다.
- CSS와 JavaScript를 기능별 영역으로 명확하게 구분한다.
- JavaScript는 전역 변수 남발을 피하고 하나의 App 상태 객체 또는 모듈 구조를 사용한다.
- 이미지 처리 로직과 UI 처리 로직을 분리한다.
- 다음과 같은 계층 구조를 권장한다.

### UI 계층

- 기능 메뉴
- 이미지 입력 UI
- 옵션 UI
- 미리보기 UI
- 상태 메시지
- 다운로드 버튼

### 상태 관리 계층

- 현재 선택된 기능
- 현재 선택된 이미지
- 병합 옵션
- 크기 조정 옵션
- 크롭 영역
- ICO 변환 상태

### 이미지 처리 계층

- 이미지 로드
- 이미지 병합
- 이미지 리사이즈
- 이미지 회전
- 이미지 반전
- 이미지 크롭
- ICO 생성
- Blob 변환
- 다운로드

### 공통 함수

- 파일 검증
- 색상 검증
- 숫자 검증
- 파일명 생성
- 이미지 정보 추출
- 오류 메시지 표시
- 상태 메시지 표시

- 함수는 하나의 명확한 역할만 담당하도록 작성한다.
- 충분한 주석을 작성한다.
- 주석은 "무엇을 하는가"보다 "왜 이렇게 구현했는가"가 필요한 부분에 집중한다.
- 유지보수성을 고려하여 긴 함수를 여러 함수로 분리한다.
- `try...catch`를 이용한 예외 처리를 포함한다.
- 변수 및 함수 이름은 일관된 camelCase를 사용한다.
- 상수는 대문자 또는 명확한 상수 객체로 관리한다.
- Magic Number를 최대한 제거한다.
- DOM 요소는 필요한 경우 캐싱하여 반복적인 querySelector 호출을 줄인다.
- 사용자 입력값은 반드시 검증한 후 이미지 처리 함수에 전달한다.
- 브라우저의 `URL.createObjectURL()` 사용 후 `URL.revokeObjectURL()`을 호출하여 메모리 누수를 방지한다.

------------------------------------------------

## 13. 테스트

### 테스트 방법

- Chrome 최신 버전에서 테스트
- Edge 최신 버전에서 테스트
- Safari 최신 버전에서 테스트
- Firefox 최신 버전에서 테스트
- Windows 및 macOS에서 테스트
- 데스크톱/태블릿/모바일 화면 크기 테스트
- 파일 드래그 앤 드롭 테스트
- 대용량 이미지 테스트
- 투명 PNG 테스트
- 다양한 이미지 비율 테스트
- 잘못된 파일 입력 테스트

### 최소 확인해야 할 시나리오

1. PNG 이미지 1장을 정상적으로 불러올 수 있는지 확인
2. JPG 이미지를 정상적으로 불러올 수 있는지 확인
3. 여러 이미지를 드래그 앤 드롭으로 불러올 수 있는지 확인
4. 2장 가로 병합이 정상적으로 작동하는지 확인
5. 2장 세로 병합이 정상적으로 작동하는지 확인
6. 3장 가로 병합이 정상적으로 작동하는지 확인
7. 3장 세로 병합이 정상적으로 작동하는지 확인
8. 4장 2×2 병합이 정상적으로 작동하는지 확인
9. 작은 크기 기준 병합이 정상적으로 작동하는지 확인
10. 큰 크기 기준 병합이 정상적으로 작동하는지 확인
11. 원본 크기 유지 병합이 정상적으로 작동하는지 확인
12. 여백 크기 및 색상이 정상적으로 적용되는지 확인
13. 테두리 굵기 및 색상이 정상적으로 적용되는지 확인
14. 투명 PNG 병합 시 투명 영역이 정상적으로 유지되는지 확인
15. 좌우 뒤집기가 정상적으로 작동하는지 확인
16. 상하 뒤집기가 정상적으로 작동하는지 확인
17. 90° 회전이 정상적으로 작동하는지 확인
18. 180° 회전이 정상적으로 작동하는지 확인
19. 270° 회전이 정상적으로 작동하는지 확인
20. 비율 크기 조정이 정상적으로 작동하는지 확인
21. 사용자 지정 W/H 입력이 정상적으로 작동하는지 확인
22. 비율 유지 상태에서 W/H 변경이 정상적으로 작동하는지 확인
23. JPEG/WebP 품질 조정이 정상적으로 적용되는지 확인
24. 크롭 영역의 8방향 핸들이 정상적으로 작동하는지 확인
25. 크롭 영역 이동이 정상적으로 작동하는지 확인
26. 크롭 영역이 이미지 바깥으로 이동하지 않는지 확인
27. 크롭 결과가 실제 원본 픽셀 기준으로 정확하게 생성되는지 확인
28. ICO 파일이 정상적으로 생성되는지 확인
29. ICO 파일에 16/32/48/64/128/256px 이미지가 포함되는지 확인
30. ICO 투명 영역이 정상적으로 유지되는지 확인
31. 결과 파일 다운로드가 정상적으로 작동하는지 확인
32. 이미지 없이 실행했을 때 적절한 오류 메시지가 표시되는지 확인
33. 잘못된 색상 코드 입력 시 오류 처리가 정상적으로 작동하는지 확인
34. 매우 큰 이미지 입력 시 브라우저가 지나치게 느려지지 않는지 확인
35. 인터넷 연결이 없는 상태에서도 핵심 기능이 작동하는지 확인

------------------------------------------------

## 14. 결과물

- 실행 가능한 단일 `index.html` 파일
- HTML/CSS/JavaScript가 하나의 파일에 포함되어 있어 별도의 빌드 과정 없이 실행 가능
- 서버 또는 백엔드 없이 이미지 처리가 가능해야 함
- 이미지 파일은 원칙적으로 브라우저 내부에서 처리
- 외부 라이브러리를 사용하는 경우 라이브러리 이름, 버전, 사용 목적 및 선택 이유를 코드 주석에 명시
- 외부 CDN에 의존하는 경우 CDN 연결이 되지 않아도 핵심 기능이 영향을 받지 않도록 설계
- 앱 사용법을 코드 내부 주석 및 화면 내 도움말로 제공

최종 결과물은 단순히 기능만 구현한 예제가 아니라 실제 배포 가능한 수준의 품질을 목표로 한다.

특히 다음 사항을 중요하게 고려한다.

- UI와 이미지 처리 로직의 분리
- 상태 관리와 DOM 조작의 분리
- 메모리 누수 방지
- 대용량 이미지 처리 성능
- 브라우저 호환성
- 키보드 및 터치 조작
- 오류 및 예외 처리
- 투명 이미지 처리
- 정확한 원본 픽셀 기준 크롭
- 고품질 이미지 리사이즈
- 안정적인 파일 다운로드
- ICO 파일의 정확한 생성
- 향후 새로운 이미지 편집 기능을 추가하기 쉬운 구조

기존 Python 프로그램의 기능과 동작 방식을 최대한 유지하되, 데스크톱 전용 기능인 파일 경로 직접 접근, 저장 위치 선택, 프로그램 종료 기능 등은 웹 브라우저 환경에 맞는 방식으로 변경한다.